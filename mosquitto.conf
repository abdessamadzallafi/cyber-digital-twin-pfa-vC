# Local demo broker. Authentication/TLS are deliberately separate from this
# development profile; use mosquitto-tls.conf only after supplying deployment
# specific certificate paths and credentials.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
